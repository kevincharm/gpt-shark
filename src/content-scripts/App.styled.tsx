import { StyledDiv } from '../common/styles'

export const StyledConsoleTitle = StyledDiv({
    position: 'absolute',
    height: '36px',
    width: '100%',
    padding: '8px 12px',
    fontSize: '16px',
    backgroundColor: '#bdbdbd'
})

export const StyledConsoleBody = StyledDiv({
    position: 'absolute',
    height: '36px',
    width: '100%',
    padding: '8px 12px',
    fontSize: '16px',
    backgroundColor: '#bdbdbd'
})
